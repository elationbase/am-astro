<div>

  <div class="steam absolute w-22.5 h-25 blur-sm opacity-85 pointer-none z-10 bottom-[14%] md:bottom-0 right-[1%]" aria-hidden="true">
    <span class="wisp"></span>
    <span class="wisp"></span>
    <span class="wisp"></span>
    <span class="wisp"></span>
  </div>
  
</div>
<style>
  .steam {
    right: 5.5%;
    mix-blend-mode: screen;
  }
  .steam .wisp {
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 26px;
    height: 38px;
    margin-left: -13px;
    background: radial-gradient(
      ellipse at 50% 60%,
      rgba(255, 255, 255, 0.95) 0%,
      rgba(255, 255, 255, 0.55) 35%,
      rgba(255, 255, 255, 0) 70%
    );
    border-radius: 50%;
    transform-origin: center bottom;
    animation: rise-steam 5s ease-out infinite;
    will-change: transform, opacity;
  }
  .steam .wisp:nth-child(1) {
    animation-delay: 0s;
    left: 50%;
  }
  .steam .wisp:nth-child(2) {
    animation-delay: 1.7s;
    left: 38%;
    animation-duration: 4.6s;
  }
  .steam .wisp:nth-child(3) {
    animation-delay: 3.2s;
    left: 60%;
    animation-duration: 5.4s;
  }
  .steam .wisp:nth-child(4) {
    animation-delay: 2.4s;
    left: 46%;
    animation-duration: 4.8s;
    opacity: 0.7;
  }

  @keyframes rise-steam {
    0% {
      transform: translate(0, 0) scale(0.55);
      opacity: 0;
    }
    15% {
      opacity: 0.9;
    }
    50% {
      transform: translate(8px, -70px) scale(1.2);
      opacity: 0.7;
    }
    100% {
      transform: translate(-6px, -160px) scale(2.1);
      opacity: 0;
    }
  }
</style>
